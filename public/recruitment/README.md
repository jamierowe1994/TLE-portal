Drop the hero photo here as `hero.jpg`.

The recruitment page probes for `/recruitment/hero.jpg` on load and uses it if
it resolves; until then it shows a Notioly figure and a small caption saying the
photo is missing. Nothing needs changing in the code — add the file and it
takes over on the next load.

Landscape works best; the frame is a rounded card at max 768px wide.
